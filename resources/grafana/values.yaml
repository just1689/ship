ingress:
  enabled: true
  annotations:
    kubernetes.io/tls-acme: "true"

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi

persistence:
  enabled: true
  size: 1Gi

sidecar:
  dashboards:
    enabled: true
  datasources:
    enabled: true
